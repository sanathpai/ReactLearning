import React from "react";

const Details = (props) => {
  return (
    <div></div>
    // <pre>
    //   <code>{JSON.stringify(props, null, 4)}</code>
    // </pre>used for debugging
  );
};
export default Details;
